A lightweight CRUD REST API built using Go's standard library (net/http) — 
no third-party frameworks or routers. Demonstrates how to handle HTTP methods, 
routing, JSON encoding/decoding, and in-memory data management using pure Go.